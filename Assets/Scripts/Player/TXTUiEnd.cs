using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TXTUiEnd : MonoBehaviour
{
    bool _flag;
    private void Update()
    {
        //foreach (var item in FindObjectsOfType<LifeHandler>())
        //{
        //    if(item.IsDead && !_flag)
        //    {
        //        GetComponent<TMPro.TextMeshProUGUI>().text = item.GetComponent<NetworkPlayer>().Nickname + " Es el perdedor";
        //        _flag = true;
        //    }
        //}
    }
}
